package com.example.farmmobileapp.feature.navigation

import androidx.compose.runtime.Composable

@Composable
fun TasksNavigationGraph(
    onTaskSelected: (String) -> Unit,
    onBackPress: () -> Unit
) {
    // Navigation graph for tasks
    // This is where you would set up your navigation for the tasks feature
    // For example, using Jetpack Navigation Compose
    // You can use the onTaskSelected callback to navigate to the TaskDetail screen
    // and pass the taskId as an argument.
}